import React from 'react';
import './Logo.scss';

const Logo = () => {
    return (
        <div className="logo">
            <a href="/" className="logo__link">
                <span className="logo__text">WishBoard</span>
            </a>
        </div>
    );
};

export default Logo;
